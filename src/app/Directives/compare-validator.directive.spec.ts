import { CompareValidatorDirective } from './compare-validator.directive';

describe('CompareValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
